<div id="<%= dom_id photo %>">

<br>
    <div class="card">
      <%= link_to(image_tag(photo.image, width: "100%"), photo.image, class: "card-img-top") %>
      <div class="card-body">
        <p class="card-text"><%= photo.legend %></p>
      </div>
    </div>

</div>
